# Run on port 8080
server.port=8080

# (Optional) Logging level
logging.level.org.springframework=INFO

# (Optional) Change server context path
# server.servlet.context-path=/api

# (Optional) Disable banner
# spring.main.banner-mode=off
